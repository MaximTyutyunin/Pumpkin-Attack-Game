extends Node2D

@onready var tile_map: TileMap = $TileMap

# Called when the node enters the scene tree for the first time.
#func _ready():
#	print("im field level")
#	EventBus.set_tilemap(tile_map)
#	
